# -*- coding: utf-8 -*-
"""
Spyder Editor

This is a temporary script file.
"""
def binary(num):
   
   isNeg = 0
   if num < 0:
       isNeg = True
       num = abs(num)
   else:
        isNeg = False

#define a function to get binary for int number.   
    def int_bin(num):
        result = ''
        if num == 0:
            result = '0'
        while num > 0:
            result = str(num%2) + result
            num = num // 2
        if isNeg:
            result = '-' + result
        return result
        
    int_part = int(num)
    decimal_part = num - int_part
    if 
    
